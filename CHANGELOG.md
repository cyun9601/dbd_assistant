# 패치노트 (CHANGELOG)

DBD 어시스턴트의 주요 변경 사항을 버전별로 기록합니다.
버전 표기는 [유의적 버전(SemVer)](https://semver.org/lang/ko/)을 따릅니다.

---

## [0.5.1] - 2026-08-31

### 용어 통일 (영→한 번역 표기)
- **공용 용어 사전 `glossary.json` 도입** — 모든 번역(자동 파이프라인 `translate_killers.py` · 서브에이전트 수동 번역)이 하나의 용어 사전을 쓰도록 했습니다. `translate_killers.py` 가 이 파일을 읽어 LLM 프롬프트 용어집을 만들므로, 이후 용어 변경은 `glossary.json` 수정만으로 반영됩니다. 번역 규칙은 `CLAUDE.md` 에 문서화했습니다.
- ***Oblivious* = 인지 불가능** — 구버전 번역 "망각"·"무지" 를 데이터 전체(perks/killers/addons)에서 치환했습니다. 옛 표기는 검색 유의어로만 유지됩니다.
- ***Skill Check* = 스킬체크** — 구 "기술 판정" 을 전면 교체했습니다. *Great Skill Check* 는 **스킬체크 대성공**, *Good Skill Check* 는 **스킬체크 성공**으로 통일 (예: 과집중 "훌륭한 기술 판정에 성공하면" → "스킬체크에 대성공하면"). "충돌 판정"(collision) 은 스킬체크와 무관하므로 그대로 두었습니다.
- ***Boon* = 호재** — 호재 토템·호재 특전·호재 부여, *Blood Favour* = **피의 가호** 표기를 정리해 glossary 에 등재했습니다.
- 용어를 바꾼 그룹은 `synonyms.js` 유의어도 갱신했습니다 — 새 용어를 그룹 맨 앞에 두고, 옛 용어("기술 판정"·"축복" 등)는 검색용으로 유지해 어느 표기로 검색해도 같은 결과가 나옵니다.

## [0.5.0] - 2026-08-12

### 퍽 데이터 (신규 챕터 + 라이브 패치 반영)
- **신규 퍽 6종 추가 — 총 315개 → 321개**(살인마 142→145, 생존자 173→176). 새 챕터의 **심판**(The Judgment) 퍽 3종 — *천상 존재의 목격* · *주술: 손아귀에 있느니* · *초토화* — 와 **오로라 스타도터**(Aurora Stardotter) 퍽 3종 — *호재: 확고부동* · *노력의 결실* · *구원의 울음소리* — 을 넣었습니다. 영어 원문·아이콘은 공식 위키에서 받고 한글 이름·설명은 기존 표기 규약(호재 토템·일반 토템·가속 상태 효과 등)에 맞춰 손번역했습니다. 아이콘도 진영별 폴더에 6개 추가(`icons/killer/` 145 · `icons/survivor/` 176).
  - 사용률은 아직 nightlight.gg 에 집계되지 않아 `nl_id`·`usage` 를 `null` 로 두었습니다(최근 챕터 퍽들과 동일하게 서버가 `/usage` 로 받아오면 채워집니다).
- **라이브 패치로 바뀐 퍽 10종의 영어 원문 갱신**, 그중 **뜻이 달라진 6종은 한글 설명도 함께 고쳤습니다**.
  - *5수 앞* — "판자 떨어뜨리기 50 % 빠르게" → **"판자를 떨어뜨린 후 50 % 더 일찍 움직임"**.
  - *반대 신문* — **추격당하지 않는 동안**이라는 조건과, 살인마가 **이동하며** 남기는 빛의 자국이라는 설명이 추가됐습니다.
  - *도움의 손* — "희생제당 + 생존자당 1회" → **생존자당 1회**로 완화.
  - *풀악셀* — 복원된 판자가 60초 동안 **모든 생존자에게 드러나는** 효과 추가.
  - *계획 변경* — 새 치료 도구 충전량 단위 오기 수정(80/90/100**초** → **%**, 위키 원문의 `Toolbox` 오타도 반영).
  - *주술: 죽음에 대한 공포* — 활성화 대상이 "무작위 토템" → **무작위 일반 토템**으로 명확해졌습니다.
- **개명 일반퍽(라이선스 만료본) 이름 정리.** 위키가 *Save the Best for Last* → *Keep Them Waiting* 처럼 비라이선스 이름을 별도 항목으로 싣기 시작해, 소유 퍽의 `former_names_en` 을 실제 짝으로 맞췄습니다 — *꺼져 가는 빛*↔`Cull the Weak`, *먹이 농락*↔`See How They Run`(둘 다 `Keep Them Waiting` 으로 잘못 적혀 있었음). 로리 스트로드 퍽 3종도 새로 기록했습니다 — *결정적인 일격*↔`Will to Live`(삶의 의지), *집착의 대상*↔`Bound by Obsession`(집착의 속박), *유일한 생존자*↔`Down to the Last`(마지막 한 명까지). 어느 이름으로 검색해도 같은 퍽이 잡힙니다.
- **아이콘 69개 갱신** — 위키에서 원본이 바뀐 퍽 아이콘을 다시 받아 webp 로 덮어썼습니다(경로 불변).

### 출시 예정 · 업데이트 예정 표시
- **라이브 설명과 예정 설명을 둘 다 저장하고, 출시일에 앱이 알아서 갈아탑니다 — 퍽 15종.** 본문에는 **지금 게임에 적용된 값**을 두고(예: *아드레날린* 가속 3초), 10.1.0 에서 바뀔 내용은 `pending` 에 담았습니다(4초). 앱은 `upcoming_date` 가 지나면 그날부터 `pending` 을 본문처럼 쓰며, 표시뿐 아니라 **키워드·의미검색·AI 정밀 검색이 모두 같은 기준**을 봅니다 — 데이터를 다시 굽지 않아도 전환됩니다. 출시 전에는 카드의 **`🔜 10.1.0 변경 예정 설명`** 버튼으로 바뀔 내용을 펼쳐 볼 수 있고, 패치 후 스크립트를 돌리면 `pending` 을 본문으로 **승격**(`PROMOTE`)하고 예정 필드를 정리합니다.
  - 10.1.0 변경분: *아드레날린*(가속 3→4초) · *전력 질주*(3→2초) · *불침번*(43/55/66→30/35/40 %) · *기술자*(소음 -8→-16미터, 페널티 5/4/3→4/3/2 %) · *돌연 중단*(80/70/60→75/60/45초) · *영광의 순간*(상자 2→1회) · *아포칼립스 독창성*(상자 2→1개, 홀드 4→3초) · *억압된 동맹*(봉쇄 30→15초) · *가장 좋은 것은 마지막에*(토큰당 -4→-5 %, 최대 24/28/32→30/35/40 %) · *간호사의 소명*(대상이 부상자→치료 중인 모든 생존자) · *광대 공포증*(30/40/50→20/25/30 %) · *소금물의 부름*(70→90초) · *숨을 곳 없음*(24→18미터) · *주술: 사냥의 전율*(8/10/12→8/9/10 %, 최대 40/50/60→40/45/50 %) · *구조*(부상 100/80/60→160/140/120초).
- **배지는 패치 출시일에 자동으로 사라집니다.** 퍽에 `upcoming`·`upcoming_patch`·`upcoming_date` 를 저장하고, 출시일은 위키 패치노트(`Patch Notes 10.1.X`)의 **Release Dates 표를 읽어** 채웁니다. 앱은 실행 시점 날짜와 비교해 배지를 그리므로 **데이터를 다시 굽지 않아도** 그날부터 평범한 설명으로 보입니다. 10.1.0 은 위키 표가 아직 TBA 라 스크립트의 `PATCH_DATES` 에 **2026-08-25** 로 적어 뒀습니다 — 표에 날짜가 올라오면 위키 값이 이를 덮고, 패치가 밀리면 그 한 줄만 고치면 됩니다.
- **"출시 예정" 과 "업데이트 예정" 을 나눠 표시합니다.** 퍽 자체가 새로 나오는 것(신규 챕터 6종 — 심판·오로라 스타도터)은 주황 `🔜 10.1.0 출시 예정`, 이미 있는 퍽의 설명만 바뀌는 것(15종)은 파랑 `🔜 10.1.0 업데이트 예정` 으로 구분합니다(`upcoming_kind`: `new` / `update`). 위키는 미출시 챕터 퍽도 안내 문구 없이 일반 퍽처럼 실어서 배너로 가려낼 수 없기 때문에, 새 챕터 캐릭터는 스크립트의 `UPCOMING_OWNERS` 에 적어 두고 출시일이 지나면 함께 자동 해제되게 했습니다.
- **무엇이 바뀌는지는 공식 패치노트를 기준으로 잡습니다.** 위키는 퍽마다 미출시 안내 문구를 빠뜨리기도 합니다(10.1.0 의 *구조* 가 그랬습니다 — 배너가 없어 라이브 변경으로 처리됐습니다). 그래서 `patchnotes.json` 의 *perk updates* 목록(`perk_updates`)을 함께 대조해, 아직 안 나온 패치에서 바뀌는 퍽을 빠짐없이 `update` 로 표시합니다 — 10.1.0 은 공식 15종과 **정확히 일치**합니다.
- **AI 정밀 검색도 같은 기준을 봅니다.** 코퍼스가 프런트와 동일한 규칙으로 라이브/예정 설명을 고르고, 퍽 이름 옆에 `설명은 현재 라이브 기준 · 10.1.0 패치에서 변경 예정`(바뀔 퍽) 또는 `10.1.0 패치로 새로 나올 퍽(아직 게임에 없음)`(신규 퍽)을 붙여 LLM 이 상황을 알고 답하게 했습니다.

### 📕 살인마 도감 — 심판 추가
- **신규 챕터 살인마 `심판`(The Judgment) 을 도감에 넣었습니다 — 43명 → 44명, 애드온 860 → 880개.** 파워는 *신들의 의지*(Will of the Gods) 이고 애드온 20종(일반 4 · 고급 5 · 희귀 5 · 매우 희귀 4 · 이리데슨트 2)을 함께 담았습니다. 아이콘 22개(초상화 · 파워 · 애드온 20)도 위키에서 받아 webp 로 저장했습니다. 지금까지는 퍽만 갱신돼 있어서 심판 퍽 3종은 검색되는데 도감에는 없는 상태였습니다.
  - 개요·파워 전문과 애드온 이름/설명은 기존 표기 규약에 맞춰 손번역했습니다 — *Divine Light* → **신성한 빛**, *Heresy/Heretic* → **이단/이단자**, *Exile* → **유형지**, *Zealous* → **광신**, *Shrines of Judgment* → **심판의 성소**, *Searcher's Pendant* → **탐구자의 펜던트**. 상태 효과·용어는 데이터에 이미 쓰던 표기를 그대로 씁니다(소진 · 은신 · 빈사 · 기술 판정 · 공포 반경).
  - 기존 43명·860개 레코드는 **한 건도 바뀌지 않았습니다**(순수 추가).
- **도감에도 `🔜 출시 예정` 배지.** 퍽 카드가 쓰던 배지를 살인마 타일·상세에서 그대로 재사용합니다(`upcomingChipHtml` 에 툴팁 명사만 인자로 추가). 심판은 10.1.0 에서 나오므로 `🔜 10.1.0 출시 예정 (8/25)` 이 붙고, **출시일이 지나면 데이터를 다시 굽지 않아도 그날부터 사라집니다** — 퍽과 같은 규칙(`upcoming`·`upcoming_patch`·`upcoming_date`)을 씁니다.
- **`update_killers_from_wiki.py` — 미출시 챕터 처리.** 퍽 쪽 `UPCOMING_OWNERS` 에 대응하는 **`UPCOMING_KILLERS`** 를 넣었습니다(위키가 미출시 살인마도 목록에 그냥 실어서 구조만으로는 못 가려냅니다). 위키가 다음 패치 기준으로 미리 고쳐 둔 설명에 붙이는 **안내 문구도 개요·파워·애드온에서 걷어냅니다** — 심판 애드온 20개 전부에 이 문구가 붙어 있어, 그대로 뒀으면 설명 앞머리에 섞여 들어갔습니다.

### ⚡ 의미 기반 AI — 코퍼스 벡터를 미리 구움
- **모드에 들어갈 때의 기다림을 없앴습니다.** 지금까지는 `의미 기반 AI` 를 누를 때마다 ① 113MB 임베딩 모델 로드를 기다리고 ② 퍽 321개를 브라우저에서 임베딩했습니다. ②는 IndexedDB 에 캐시됐지만 ①이 **항상 진입 경로를 막고** 있어서(모델을 먼저 `await`) 앱을 켤 때마다 느렸습니다.
- **`build_embeddings.py` · `embeddings/` 신규.** 퍽 데이터가 그대로면 코퍼스 임베딩 결과도 항상 같으므로 미리 구워 둡니다 — `index.json`(메타 · 퍽 id 순서 · 패시지 해시)과 검색 프로파일별 `*.bin`(float32 321×384, 각 482KB). 프로파일은 검색 범위·표시 언어 조합 3종입니다: `multi`(한+영, 기본) · `same-ko` · `same-en`. 프런트의 `semanticPassage()` 와 **같은 규칙**으로 패시지를 만들고 같은 q8 ONNX 모델·평균 풀링·L2 정규화를 씁니다.
- **모델은 질의를 임베딩할 때만 기다립니다.** 코퍼스는 파일에서 읽고, 모델 로드는 `await` 하지 않고 배경에서 예열합니다 — 모드 진입은 482KB 읽기로 끝나고, 모델은 사용자가 검색어를 치는 동안 로드됩니다. 파일이 없거나 깨졌으면 **예전 경로(IndexedDB 캐시 → 그 자리에서 임베딩)로 폴백**하므로 동작이 끊기지 않습니다.
- **낡은 벡터는 스스로 고칩니다.** 출시일이 지나 `pending` 설명으로 갈아타는 퍽(10.1.0 의 15종)은 패시지가 바뀌어 구운 벡터가 낡습니다. 메타에 적어 둔 **패시지 해시**(FNV-1a 32bit, 파이썬·JS 동일 구현)를 대조해 **달라진 행만** 모델이 준비된 뒤 다시 임베딩합니다 — 파일을 다시 굽지 않아도 틀린 결과가 나오지 않습니다.
- **모델·양자화가 다르면 파일을 쓰지 않습니다.** 메타의 `model`·`dtype` 을 프런트 설정과 대조해, 어긋나면 조용히 잘못된 벡터를 쓰는 대신 폴백합니다(`DTYPE` 상수를 빼서 `pipeline()` 과 검문이 같은 값을 보게 했습니다).

### 📜 패치노트 (신규 기능)
- **패치노트 탭 추가.** 앱 상단에 **🔍 퍽 검색 / 📕 살인마 도감 / 📜 패치노트** 세 번째 탭이 생겼습니다. 패치를 고르면(최신순 · PTB 는 따로 표시) 공식 노트를 제목·날짜·원문 링크와 함께 보여줍니다. 제목/소제목/중첩 불릿을 그대로 살려 렌더링하고, 개발자 노트(*Dev note*)도 기울임 그대로 남습니다.
- **그 패치에서 바뀐 퍽을 바로 펼쳐 봅니다.** 각 패치가 언급한 퍽을 **한글 이름 칩**으로 모아 보여주고(예: 10.1.0 PTB — 21개), 누르면 그 자리에서 퍽 카드가 펼쳐집니다(즐겨찾기·태그도 그대로 동작). 공식 노트가 라이선스 만료 이름(*Keep Them Waiting*)을 써도 `former_names_en` 으로 대조해 *가장 좋은 것은 마지막에* 로 이어 줍니다.
- **`update_patchnotes_from_steam.py` · `patchnotes.json` 신규.** Steam 공식 공지(스토어 뉴스와 같은 원문)에서 패치노트만 추려 굽습니다 — 현재 **15개 패치**(9.5.0 ~ 10.1.0 PTB). 본문 BBCode 를 앱이 그릴 수 있는 최소 HTML(`<b>`/`<i>`/`<a>`)과 블록 목록(h2/h3/p/li + 들여쓰기)으로 바꾸고, 오프라인에서 못 받는 이미지는 버립니다. 서버 `GET /patchnotes` 로 제공하고 빌드 스펙(`dbd.spec`)에도 포함했습니다.
  - **본문은 공식 원문(영어)** 입니다 — Steam 에 한국어판 공지가 없어(`l=koreana` 로 받아도 영어) 원문을 그대로 싣고, 대신 퍽 칩·UI 는 한글로 안내합니다.

### 도구
- **정적 파일에 `Cache-Control: no-cache`.** `index.html`·`*.json`·`*.js`·`*.bin` 을 매번 재검증하게 했습니다. 브라우저(특히 exe 의 WebView2)가 `index.html` 을 캐시해 두면 데이터·화면을 고쳐도 앱을 새로 켤 때까지 옛 화면이 그대로 떠서, 갱신이 안 된 것처럼 보였습니다. 변경 없으면 304 라 비용은 거의 없습니다. `*.bin`(구워 둔 임베딩 행렬)도 같이 넣었습니다 — 메타(`index.json`)와 짝이 맞아야 하는데 한쪽만 캐시되면 어긋나기 때문입니다.
- **`update_en_from_wiki.py` — 미출시 패치 설명 처리.** 안내 문구는 설명에서 걷어내고(그대로 두면 본문에 섞여 들어갑니다) 해당 퍽에 출시 예정 표시를 붙입니다. 출시일이 지났거나 위키가 안내 문구를 떼면 표시를 자동으로 해제합니다. 실행 끝에 `UP` 목록으로 "한글 설명도 맞춰 달라"고 알려 줍니다. 아이콘을 다시 받지 않는 `--no-icons` 도 추가했습니다(설명만 갱신할 때).
- **신규 퍽 누락 감지.** 지금까지는 *perks.json 에 있는데 위키에 없는* 퍽만 보고해서, 반대 방향(위키에 새로 생긴 퍽)을 놓쳤습니다. 이제 위키에만 있는 퍽을 `NEW`(수동 추가 필요) 와 `TWIN`(`former_names_en` 으로 이미 커버된 개명 일반퍽) 으로 나눠 끝에 보고합니다.

---

## [0.4.1] - 2026-07-09

### 번역·표기
- **상태 효과 표기 통일 — Elusive → `신출귀몰 효과`.** 회피/은밀로 갈려 있던 *Elusive Status Effect* 의 한글 표기를 **신출귀몰 효과**로 통일했습니다. 퍽 4종(자기 보호·우리만의 공간·반대 신문·뛰어난 지각력)과 애드온 1종(무뎌진 칼)에 반영했으며, `desc_html`·`desc_text`·`search_blob` 세 필드를 함께 갱신했습니다. 은신(stealth) 의미로 쓰인 `은밀`(에이리언의 직감 등)은 다른 개념이라 그대로 두었습니다.
- **상태 효과 표기 통일 — Exposed → `약점 노출 효과`.** *Exposed Status Effect* 의 한글 표기를 **약점 노출 효과**로 통일했습니다. 퍽 14종·애드온 8종·살인마 2종(고스트 페이스·기사)에 반영했습니다. 앞서 개별 변경했던 *드래곤의 손아귀* 의 `약점 노출` 과 일관되게 맞추고, 여러 상태 이상을 함께 나열하는 *불침번* 은 공유하는 `상태 효과` 표기를 유지했습니다. 오라가 드러나는(reveal) 의미의 `노출`(*숨을 곳 없음* 등)은 다른 개념이라 건드리지 않았습니다.

---

## [0.4.0] - 2026-07-03

### 살인마 도감 (신규 기능)
- **📕 살인마 도감 추가.** 퍽 검색 외에 **살인마별 개요·파워·애드온**을 볼 수 있는 도감 화면을 새로 넣었습니다. 앱 상단에 **🔍 퍽 검색 / 📕 살인마 도감** 섹션 탭이 생기고, 도감 모드는 **살인마 그리드 → 상세(개요 · 파워 아이콘+설명 · 등급별 애드온 그리드)** 로 렌더링됩니다. 살인마 **43명**, 애드온 **860개**를 담았습니다.
  - 표시 언어(한/영) 토글을 그대로 재사용하며, 한글 필드가 비어 있으면 **영어로 자동 폴백**합니다. 애드온은 등급(common·uncommon·rare·very rare·ultra rare)별로 묶어 보여주고, 색강조(Highlight)는 퍽 카드의 CSS 를 공유합니다.

### 살인마·애드온 데이터
- **`killers.json`(43) · `addons.json`(860) 신규.** 퍽과 같은 [공식 위키](https://deadbydaylight.wiki.gg)(deadbydaylight.wiki.gg) MediaWiki API 에서 살인마별 **개요·파워·애드온의 영어 원문과 아이콘**을 수집했습니다. 퍽 규약(한/영 병기, `desc_html`/`desc_text` + `_en`, `search_blob`, 수동 필드 보존)을 그대로 계승하고, 살인마당 1개(`killers.json`)와 애드온당 1개(`addons.json`, 평탄)로 나눠 `addon_ids`/`killer_id` 외래키로 연결했습니다.
  - **한글 번역 전량 완료** — 43명 살인마의 개요·파워 설명과 860개 애드온의 이름·설명을 모두 한글로 번역해 채웠습니다(공식 현지화가 아닌 자체 번역, 영어 원문은 표시 언어 토글로 확인).
  - **아이콘 신규 폴더** — `icons/killer_portrait/`(초상화)·`icons/power/`(파워)·`icons/addon/`(애드온) 을 추가했습니다. 초상화·파워는 살인마 id 로, 애드온은 위키 원본 파일명(`IconAddon_*`)으로 명명해 재실행해도 경로가 바뀌지 않습니다(diff 최소화).

### 도구·구조
- **`update_killers_from_wiki.py` 추가.** 퍽 갱신 도구(`update_en_from_wiki.py`)의 검증된 유틸(`fetch`·`clean_html`·`save_icon_webp` 등)을 재사용해, 위키에서 살인마·애드온의 영어·아이콘을 다시 굽는 재실행 가능한 스크립트입니다. 재빌드 시 손번역한 한글·별칭 필드는 **id 기준으로 보존**하고 `*_en`·아이콘만 최신본으로 덮어씁니다.
- **`ko_merge.py` 추가.** 영어 원문을 청크로 나눠 번역한 뒤 병합하는 도구 — HTML 색강조·불릿을 보존하고 `_text`·`search_blob`(한/영 통합, 추후 도감 검색용)을 다시 굽습니다. API 자동 번역용 `translate_killers.py` 도 함께 넣었습니다.
- **서버 엔드포인트 `GET /killers`·`/addons` 추가.** 시작 시 두 파일을 로드해(없으면 기능만 비활성) 프런트에 정적 제공합니다. 빌드 스펙(`dbd.spec`)에도 두 JSON 과 신규 아이콘 폴더를 번들에 포함했습니다.
- 설계 근거는 [`docs/killers_addons_design.md`](docs/killers_addons_design.md) 에 정리했습니다.

---

## [0.3.2] - 2026-06-26

### 퍽 데이터
- **한글 퍽 설명을 공식 위키 표기로 전면 재작성.** v0.3.1 에서 영어 설명·아이콘을 [공식 위키](https://deadbydaylight.wiki.gg/wiki/Perks)(deadbydaylight.wiki.gg)로 옮긴 데 이어, 이번엔 **한글 설명문(`desc_text`·`desc_html`)과 검색 텍스트(`search_blob`)를 315개 퍽 전부** 위키 표기에 맞춰 다시 작성했습니다.
  - 기존의 서술형 번역 대신 **티어별 수치(예: 6/7/8 토큰, 10/12.5/15 %)·상태 효과 이름·강조 표기**를 그대로 살려, 영어 원문과 1:1로 대응하고 실제 게임 수치를 정확히 보여줍니다.
  - 플레이버 대사(인용구)도 위키 표기로 통일하고 화자 이름을 정리했습니다(예: *–샘 루미스 박사* → *— 허먼 카터, 닥터*).
- **셰인 위그와스 퍽 2개 이름 정정** — *도움의 손길 → 도움의 손*, *전속력 → 풀악셀*. *CHAPTER 40.5: The Life Road* 정식 출시 표기에 맞췄습니다.
- **🔜 출시 예정 배지 해제.** *CHAPTER 40.5: The Life Road* 가 **2026-06-25 출시**됨에 따라, **셰인 위그와스 퍽 3개**(반대 신문·도움의 손·풀악셀)의 `upcoming` 플래그를 제거했습니다. 더 이상 카드에 "출시 예정" 배지가 표시되지 않습니다.

---

## [0.3.1] - 2026-06-21

### 퍽 데이터
- **데이터 출처를 공식 위키로 갱신 (deadbydaylight.wiki.gg).** 기존 dbd-db.com 보다 최신·정확한 [공식 위키](https://deadbydaylight.wiki.gg/wiki/Perks)에서 살인마·생존자 **퍽 아이콘과 영어 설명문을 모두 최신화**했습니다(기존 306개 전부). 아이콘은 위키 원본(PNG)을 webp 로 변환해 기존 경로에 덮어쓰고, 영어 설명은 위키 표기 그대로(티어별 수치·강조 포함) 정제해 반영했습니다. **한글 이름·설명 등 다른 필드는 그대로 유지**합니다.
  - 라이선스 문제로 일반 퍽이 된 항목(예: *가장 좋은 것은 마지막에* ↔ *Keep Them Waiting*)은 위키의 "Identical to …" 참조를 따라가 실제 설명을 가져오고, 본문 속 퍽 이름은 우리 표기로 치환했습니다.
  - 재실행 가능한 갱신 도구 **`update_en_from_wiki.py`** 를 추가했습니다 — 패치 후 다시 돌리면 모든 퍽의 영어 설명·아이콘이 위키 기준으로 갱신되며(이름으로 매칭), 직접 수정한 한글 필드는 건드리지 않습니다.
  - 갱신 과정에서 기존 데이터 오류도 바로잡았습니다 — *Teamwork: Toughen Up* / *Throw Down* 두 퍽의 아이콘이 서로 뒤바뀐 채 저장돼 있던 문제 등.
  - **사용자에게 보이는 출처 표기도 갱신.** 앱 푸터와 `README` 의 "데이터 출처" 안내를 **영어 설명·아이콘 = deadbydaylight.wiki.gg · 한글 = dbd-db.com · 사용률 = nightlight.gg** 로 나눠 표기했습니다. 위키로 옮긴 건 영어·아이콘이고 한글 이름·설명은 여전히 dbd-db.com 출처라 양쪽을 모두 크레딧합니다. `README` 의 퍽 개수 표기도 306 → **315**(살인마 142 + 생존자 173)로 맞췄습니다.
- **신규 퍽 9개 추가.** 신규 캐릭터 3명의 퍽을 추가해 총 **315개**가 되었습니다.
  - **제이슨 부히스(슬래셔)** — 주술: 죽음의 공포 · 광란 · 고요한 그림자
  - **권태영** — 우리의 안식처 · 다섯 수 앞 · 몰입 상태
  - **셰인 위그와스** — 반대 신문 · 도움의 손길 · 전속력
  - 한글 이름·설명은 기존 데이터의 용어(주술·집착·근육 손상·감지 불가능 등)에 맞춰 번역했습니다(공식 현지화가 아닌 자체 번역). 영어 원문은 🇬🇧 버튼에서 그대로 확인할 수 있습니다.
- **🔜 출시 예정 배지.** 아직 출시되지 않은 퍽을 카드에 **출시 예정** 배지로 따로 표시합니다. 현재는 *CHAPTER 40.5: The Life Road*(2026-06-25 출시 예정)의 **셰인 위그와스 퍽 3개**에 적용됩니다(`perks.json` 의 `upcoming` 플래그 기반).

### 검색
- **별칭·줄임말로도 검색.** 커뮤니티에서 흔히 쓰는 **줄임말·별칭**으로도 퍽을 찾을 수 있게 했습니다 — 예: *불안한 존재* → `불존`, *사망 공포증* → `타나토`, *인내심* → `엔듀링`, *재앙의 갈고리: 고통의 울림* → `원깜`, *피의 교도관* → `잼든`/`워든`. 별칭은 닉네임이라 언어와 무관해, **표시 언어·검색 범위와 상관없이** 어느 쪽으로 입력해도 매칭됩니다. 살인마 퍽 44개에 별칭을 달았습니다(`perks.json` 의 `aliases`).
- **예전(개명 전) 이름으로도 검색.** 업데이트로 이름이 바뀐 퍽을 옛 이름으로 기억하는 경우를 위해 **예전 이름**도 매칭에 넣었습니다 — 예: *서지* ← `철렁`, *잔혹한 속박* ← `폐소공포증`, *마인드 브레이커* ← `공포 조장`(*Fear Monger*). 한글 옛 이름(`former_names`)은 한국어 이름에, 영문 옛 이름(`former_names_en`)은 영어 이름에만 붙어 **검색 범위에 맞게** 동작합니다(9개 퍽).
- 별칭·예전 이름은 **세 검색 모드 모두**(키워드·의미기반·AI 정밀)에 반영됩니다 — 키워드 검색은 이름 필드에 합쳐 매칭하고, 의미기반 검색은 임베딩 패시지 뒤에 덧붙이며, AI 정밀 검색은 코퍼스의 퍽 이름 옆에 `(별칭 …; 예전 이름 …)` 형태로 함께 보냅니다.

### 동작·구조
- **퍽 데이터 단일화 — `perks_data.js` 제거.** 같은 데이터를 두 벌(`perks.json` + `perks_data.js`)로 들고 동기화하던 구조를 정리했습니다. 이제 원본은 **`perks.json` 하나**이고, 프런트엔드는 로컬 서버의 새 엔드포인트 **`GET /perks`**(서버가 `perks.json` 을 읽어 제공)에서 퍽 데이터를 받아옵니다.
  - 이에 따라 앱은 **로컬 서버 실행이 필수**가 됩니다(기존 `file://` 더블클릭 단독 실행은 더 이상 지원하지 않음). `build_data.py`·빌드 스펙(`dbd.spec`)·`README` 에서도 `perks_data.js` 참조를 제거했습니다.
- **손으로 채운 필드 전체 재빌드 보존.** `build_data.py` 로 `perks.json` 을 통째로 다시 구워도, 수동 입력 필드(`aliases`·`former_names`·`former_names_en`·`upcoming`)는 기존 파일에서 **id 기준으로 이어받아** 유실되지 않습니다(새 퍽은 빈 값으로 시작). 위키 갱신 도구(`update_en_from_wiki.py`)와 별개로, 데이터를 처음부터 다시 만들 때도 별칭·예전 이름·출시예정 표시가 그대로 남습니다.

---

## [0.2.3] - 2026-06-15

### 검색
- **검색 범위 기본값을 '다국어'로 변경.** 처음 실행 시(저장된 설정이 없을 때) 검색 범위가 **다국어 검색**(한국어·영어 모두 매칭)으로 시작합니다. 어느 언어로 입력해도 바로 매칭되도록 한 변경으로, 기존엔 표시 언어와 같은 언어로만 찾는 **동일 언어 검색**이 기본이었습니다.
  - ⚙️ 설정에서 직접 **동일 언어 검색**으로 바꾼 적이 있으면 그 선택은 그대로 유지됩니다(이 브라우저 `localStorage`의 `dbd-scope` 값).

---

## [0.2.2] - 2026-06-15

### 검색
- **🎙️ 음성 검색 (전역 단축키 + STT).** 게임이 풀스크린이라 창 전환이 어려울 때, **전역 단축키 `Ctrl+Shift+Space`** 를 눌러 마이크로 퍽 효과를 말하면 음성 인식으로 받아써 검색창에 넣고 **현재 검색 모드로 자동 검색**합니다. 한 번 누르면 녹음 시작, 다시 누르면 종료·인식(토글)이며, 깜빡 잊어도 20초가 지나면 자동 종료합니다. 게임 화면 위에서도 상태를 알 수 있게 **부드러운 차임음**(시작=상승·종료=하강·성공=상승 아르페지오·오류=하강)으로 피드백합니다 — 사인파+감쇠 엔벨로프로 합성해 메모리에서 재생하며(거슬리는 사각파 비프음이 아님), 외부 의존성 없이 stdlib 만 씁니다.
  - 전역 단축키는 게임이 포그라운드여도 동작하도록 **Win32 `RegisterHotKey`** 를 `ctypes` 로 직접 호출합니다(추가 의존성 없음). OS 가 단축키를 가로채므로 게임에는 전달되지 않습니다. 창이 포커스됐을 땐 검색창 옆 **🎙️ 버튼**으로도 같은 동작을 쓸 수 있습니다.
  - 음성 인식은 **OpenAI 음성 인식 API**(`gpt-4o-mini-transcribe`, 한국어)를 사용합니다 — AI 정밀 검색과 **같은 OpenAI 키**(⚙️ 설정 또는 `OPENAI_API_KEY`)면 됩니다. 키가 없으면 녹음 전에 안내합니다.
  - 마이크 녹음은 `sounddevice`(16kHz·모노, PortAudio 번들)로 하고 stdlib `wave` 로 인코딩합니다(numpy 불필요). 단축키 신호와 인식 결과는 서버가 **SSE(`/events`)** 로 보내, 네이티브 창·브라우저 두 실행 형태 모두에서 동작합니다.
  - `run.bat` 은 `sounddevice` 를 자동 설치하고, exe 빌드(`dbd.spec`)는 PortAudio DLL 과 함께 번들합니다. 단축키를 바꾸려면 `voice.py` 의 `HOTKEY_*` 상수만 고치면 됩니다.
  - **무음/장치 진단.** 녹음이 됐는데 인식 결과가 비는 경우의 원인을 바로 알 수 있게, 캡처한 오디오의 **피크 음량(%)** 과 **실제 입력 장치 이름**을 측정해 안내합니다. 음량이 사실상 0%면(잘못되거나 음소거된 입력 장치 — 예: 가상 마이크가 기본 장치로 잡힌 경우) OpenAI 호출 없이 *"마이크 입력이 거의 감지되지 않았습니다(볼륨 0%) · 현재 장치: …"* 로 안내하고, 음량은 정상인데 빈 결과면 *"소리는 들어왔지만 … 못 알아들음"* 으로 구분합니다. 음량 측정은 stdlib `array` 만 써서 numpy 없이(파이썬 3.13 에서 제거된 `audioop` 도 쓰지 않음) 동작합니다.
  - **입력 장치 선택 UI.** ⚙️ 설정에 **🎙️ 음성 검색 입력 장치** 드롭다운을 추가했습니다. 기본 입력이 가상 마이크로 잘못 잡혀 무음만 녹음되던 문제를, 실제 마이크를 직접 골라 해결할 수 있습니다(비워두면 시스템 기본). 선택은 `voice.json` 에 저장되어 다음 실행에도 유지되고, 인덱스가 아니라 **장치 이름**으로 저장·해석해 재부팅/재연결에도 안정적입니다. 목록은 16kHz 녹음이 실제로 열리는 **MME/DirectSound** 장치만(이름이 온전한 DirectSound 우선) 보여주고 호스트 API 중복을 합쳐 깔끔하게 정리합니다 — WASAPI/WDM-KS 항목은 이 녹음 방식(16k 블로킹 스트림)으로 열리지 않아 제외합니다.

---

## [0.2.1] - 2026-06-11

### 화면
- **라이트/다크 모드 전환.** 헤더에 테마 토글 버튼(☀️/🌙)을 추가했습니다. 다크일 땐 ☀️(라이트로 전환), 라이트일 땐 🌙(다크로 전환)을 보여줍니다. 선택은 이 브라우저(`localStorage`)에 저장되어 다음 실행에도 유지됩니다.
  - 첫 페인트 전 인라인 스크립트가 **저장값 → 시스템 설정(`prefers-color-scheme`) → 다크** 순으로 테마를 정해, 새로고침 시 잘못된 색 깜빡임이 없습니다.
  - 색은 CSS 변수(팔레트)만 갈아끼우는 방식이라, 카드·하이라이트·즐겨찾기·설정 모달까지 일관되게 라이트 테마로 전환됩니다.

### 검색
- **AI 정밀 검색 누락 개선.** 매칭이 가끔 몇 개씩 빠지던 문제를 두 가지로 보완했습니다.
  - 매칭 프롬프트를 *정밀(precision) 우선 → 재현율(recall) 우선* 으로 변경. 포함 여부가 애매하면 빼지 않고 **낮은 confidence(예: 40 미만)로라도 포함**하고, 명백히 무관한 퍽만 제외합니다(거르는 일은 사용자가 confidence로). 누락보다 과포함이 낫다는 방향입니다.
  - LLM 출력 상한 `MAX_OUTPUT_TOKENS` 를 2048 → **8192** 로 상향. Opus의 adaptive thinking 이 사고 토큰과 출력 예산을 나눠 써서, 사고가 길어진 회차에 결과 JSON이 잘려 매칭이 누락되던 현상을 줄입니다. 상한일 뿐이라 실제 생성한 토큰만 과금되어 평소 비용은 그대로입니다.
  - 출력이 상한에 도달해 잘리면(`stop_reason`/`finish_reason`) 진단용 경고를 로깅합니다.
- 향후 단계로 **Retrieve-then-Rerank**(후보 추출 → LLM 재랭킹) 설계 메모(`docs/retrieve-then-rerank.md`)를 추가했습니다(아이디어 단계, 미구현).

### 문서·라이선스
- **PolyForm Noncommercial License 1.0.0** 적용. 개인·학습·연구·취미 및 비영리/교육/공공 기관에서의 사용·수정·재배포는 허용하고, 상업적 이용은 금지합니다. README 에 라이선스 안내를 추가했습니다.
  - 본 라이선스는 **이 저장소의 코드**에 적용되며, *Dead by Daylight* 관련 자산(퍽 데이터·아이콘·명칭 등)의 권리는 Behaviour Interactive 에 있습니다.

---

## [0.2.0] - 2026-06-09

### 검색
- **퍽 사용률 표시 (nightlight.gg).** 각 퍽 카드에 실제 게임에서 얼마나 많이 쓰이는지 **🔥 사용률 %** 를 등급색과 함께 표시합니다(매우 높음·높음·보통·낮음). 데이터는 [nightlight.gg](https://nightlight.gg/perks) 의 커뮤니티 통계(최근 약 4주, 매일 1회 갱신)에서 가져옵니다.
- **전체 보기 정렬 — 사용률순 기본.** 📖 전체 보기를 열면 **가장 많이 쓰이는 퍽이 맨 위**로 정렬됩니다. **🔥 사용률순 / 가나다순** 토글로 전환할 수 있습니다.
- **검색 결과 동점 정렬.** 키워드·의미·AI 정밀 검색에서 관련도 순서는 그대로 두고, **점수가 같을 때만** 사용률 높은 퍽을 앞으로 보냅니다(관련도가 사용률에 밀리지 않음).

### 퍽 정보
- **영어 원문 보기.** 각 퍽 카드에 **🇬🇧 영어 원문** 버튼을 추가했습니다. 한글 번역이 의심스러울 때 펼치면 영어 퍽 이름과 원문 설명을 그대로 볼 수 있습니다. dbd-db 로컬라이제이션에서 영어(`en`)도 함께 받아 `perks.json`/`perks_data.js` 에 구워 두므로 서버 없이(`file://`)도 동작합니다.

### 언어 설정 (⚙️)
- **표시 언어 (한국어/English).** ⚙️ 설정에서 퍽 이름·소유자·설명을 보여줄 언어를 고릅니다. **표시 방식만** 바꾸며 검색 동작과는 무관합니다. 영어로 두면 카드의 원문 버튼은 반대로 **🇰🇷 한국어**(번역)를 펼쳐 보여줍니다. 예시 칩·입력창 안내문도 선택 언어로 바뀝니다.
- **검색 범위 (동일 언어 / 다국어).** 검색 시 어떤 언어로 매칭할지 두 버튼 중 하나로 고릅니다.
  - **동일 언어 검색**: 표시 언어와 같은 언어로만 검색.
  - **다국어 검색**: 한국어·영어 모두로 검색 — 어느 언어로 입력해도 매칭됩니다.
  - 세 검색 모드 모두에 적용됩니다(키워드·의미기반·AI 정밀). AI 정밀 검색은 선택 범위에 맞춰 프롬프트 코퍼스를, 의미기반은 임베딩을 범위별로 따로 캐시합니다.
- 두 설정은 이 브라우저(localStorage)에 저장되어 다음 실행에도 유지됩니다.

### 동작·구조
- 로컬 서버에 **`/usage`** 엔드포인트 추가 — nightlight 통계를 받아 우리 퍽 id 로 매핑해 캐시(6시간)합니다. 서버 실행 시 매일 갱신되는 "실시간" 값을, 서버가 없거나 오프라인이면 빌드 때 구워 둔 기준값을 사용합니다(`file://` 더블클릭에서도 동작).
- 빌드 스크립트(`build_data.py`)가 각 퍽에 nightlight 고정 id(`nl_id`)와 기준 사용률을 함께 구워 둡니다. nightlight 숫자 id 는 사이트 배포와 무관하게 고정이라, 런타임에는 안정적인 사용률 API 만 호출합니다.
- nightlight 연동 로직은 `nightlight.py` 로 분리(외부 의존성 없음). 수집이 실패해도 퍽 데이터 빌드와 검색 기능에는 영향이 없습니다.

---

## [0.1.2] - 2026-06-09

### 배포·실행
- **자동 업데이트 확인.** 앱 시작 시 GitHub 릴리스에서 최신 버전을 확인해, 새 버전이 있으면 상단에 알림 배너를 띄웁니다. **⬇ 업데이트** 버튼으로 릴리스 다운로드 페이지를 기본 브라우저에서 열고, **✕** 로 해당 버전 알림을 끌 수 있습니다(더 새 버전이 나오면 다시 표시).
  - 현재 버전은 `version.py` 한 곳에서 관리하고, 푸터에도 `v0.1.2` 로 표시합니다.
  - 네트워크가 없거나 GitHub 응답이 늦어도 조용히 넘어가며, 검색 기능에는 영향이 없습니다.

### 이름 변경
- 앱 이름을 **DBD 퍽 검색기 → DBD 어시스턴트** 로 변경. 창·탭 제목과 헤더, 푸터 버전 표기에 반영됩니다.
- 배포 폴더·실행 파일 이름도 **`DBD-Perk-Finder` → `DBD-Assistant`** 로 변경(`DBD-Assistant\DBD-Assistant.exe`). 기존 `DBD-Perk-Finder` 폴더는 지워도 됩니다 — API 키·즐겨찾기·태그 등 사용자 데이터는 `%APPDATA%\dbd-assistant\` 에 있어 그대로 유지됩니다.

### 아이콘
- **앱 전용 아이콘 적용.** 실행 파일(작업표시줄·탐색기)과 네이티브 창에 전용 아이콘(`assets/icon.ico`)을 표시합니다. 창 아이콘 파일이 없으면 exe 기본 아이콘으로 자연스럽게 폴백합니다.

---

## [0.1.1] - 2026-06-09

### 검색
- **전체 퍽 보기** — 진영 토글 옆에 **📖 전체** 버튼 추가. 검색·모드와 무관하게 현재 역할의 모든 퍽을 이름순으로 모아 봅니다. 검색 결과가 없는 빈 화면에서도 "퍽 전체 보기" 버튼으로 바로 둘러볼 수 있습니다.
- **AI 정밀 검색에 태그 반영** — LLM 코퍼스에 사용자가 단 **태그**를 함께 전달해, 질문이 태그와 맞으면 강하게 매칭합니다. 퍽 이름과 겹치는 질문은 최우선으로 포함하도록 검색 프롬프트를 보강했습니다.
- 태그를 편집하면 코퍼스를 즉시 재생성해 다음 검색부터 새 태그가 반영됩니다(프롬프트 캐시는 1회 미스 후 재캐싱).

### 수정
- **바비큐 & 칠리** 퍽 이름이 `&` 로 깨져 보이던 문제를 `&` 로 교정.
- `run.bat` 실행 오류 및 `build.bat` 빌드 스크립트 수정 (`.gitattributes` 추가).

### 문서
- README 데모 영상(GIF·MP4) 업데이트, 미사용 스크린샷 정리.

---

## [0.1.0] - 2026-06-08

첫 공개 버전. 게임 중 겪은 퍽 효과를 한국어로 설명하면 가장 비슷한 퍽을 찾아주는 검색기로, **파이썬 설치 없이 exe 더블클릭만으로** 실행할 수 있습니다.

### 검색
- 세 가지 검색 모드
  - **키워드 + 유의어** — 게임 용어 사전 기반, 즉시·오프라인.
  - **의미 기반 AI** — 로컬 다국어 임베딩 모델(브라우저 내 실행). 첫 사용 시 1회 자동 다운로드.
  - **AI 정밀 검색** — 퍽 전체를 LLM 에 보내 근거와 함께 매칭(Anthropic/OpenAI).
- **살인마 / 생존자 진영 토글** — 진영별 퍽·아이콘·LLM 코퍼스 분리 (살인마 139 + 생존자 167 = 306개).

### 정리·관리
- **즐겨찾기** — 퍽 카드의 ⭐ 토글, 즐겨찾기한 퍽만 모아 보기 (서버 저장, 서버 없으면 `localStorage` 폴백).
- **태그** — 퍽별 태그 달기, 태그 필터·전체 목록 보기. 기본 태그(공유)와 사용자 수정분을 병합.

### 배포·실행
- **exe 배포 (파이썬 불필요).** PyInstaller one-folder 로 파이썬·SDK 까지 묶어 배포.
  → 빌드: `build.bat`, 결과: `dist\DBD-Perk-Finder\` 폴더를 zip 으로 전달.
- **네이티브 앱 창 (pywebview).** 창을 닫으면 서버도 함께 종료. WebView2 없으면 브라우저로 폴백.
- 개발용 실행: `run.bat`(콘솔+브라우저) · `python app.py`(네이티브 창).

### API 키 · 보안
- **⚙️ 설정에서 API 키 입력.** `setx` 환경변수 설정 불필요.
  - 키는 **이 PC에만** 저장 — `%APPDATA%\dbd-assistant\config.json` 에 **Windows DPAPI** 로 암호화.
  - 평문 키는 브라우저로 노출되지 않고, 설정 화면엔 마스킹값(`sk-ant…1234`)만 표시.
- **키 우선순위: UI 저장키 > 환경변수.** UI 에 저장한 키가 없으면 `ANTHROPIC_API_KEY` /
  `OPENAI_API_KEY` 를 자동으로 가져와 쓰고, UI 에서 저장하면 그 키가 우선 적용됩니다
  (삭제하면 다시 환경변수로 복귀).
- **사용자 데이터 경로 분리** — 읽기 전용 자산은 번들에, 사용자 데이터(키·즐겨찾기·태그·다운로드 모델)는
  `%APPDATA%\dbd-assistant\` 에 저장. exe 폴더가 쓰기 불가여도 정상 동작.

### 참고
- 자체 빌드라 코드 서명이 없어 첫 실행 시 SmartScreen 경고가 날 수 있습니다 (추가 정보 → 실행).
- 의미기반 AI 모델(~155MB)은 용량 때문에 번들에 넣지 않고, 첫 사용 시 1회 자동 다운로드합니다.
